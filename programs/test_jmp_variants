$PROGRAM
    "TEST_JMP_VARIANTS"
$DATA
$CODE
    PUSH @je
    JC
je:
    PUSH @jg
    JE
jg:
    PUSH @jge
    JG
jge:
    PUSH @jl
    JGE
jl:
    PUSH @jle
    JL
jle:
    PUSH @jnc
    JLE
jnc:
    PUSH @jne
    JNC
jne:
    PUSH @jnp
    JNE
jnp:
    PUSH @jp
    JNP
jp:
    PUSH @halt
    JP
    HALT
halt:
    HALT
$END
